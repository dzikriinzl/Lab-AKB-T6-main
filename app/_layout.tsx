import { Stack } from "expo-router";

// Layout utama untuk navigasi stack.
// Karena hanya ada satu layar, ini berfungsi sebagai kontainer utama.
export default function Layout() {
  return <Stack />;
}
